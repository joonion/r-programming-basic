# AGENTS.md

## 프로젝트 목적

이 저장소는 R 프로그래밍 기초 학습 자료와 예제 코드를 관리한다.
설명과 코드는 입문자가 순서대로 따라 할 수 있도록 단순하고 명확하게 작성한다.

## 학습자 특성

- Seurat를 이용하여 전사체 분석을 연구하는 생물학 전공의 석사/박사 대학원생이다.
- 프로그래밍 경험이 거의 없고, R은 처음으로 배운다.

## 작업 원칙

- 모든 학습 자료와 설명은 기본적으로 한국어로 작성한다.
- 새로운 개념은 짧은 설명, 실행 가능한 예제, 연습 문제 순서로 소개한다.
- 예제는 한 번에 하나의 핵심 개념에 집중하고 불필요한 추상화를 피한다.
- R 기본 문법을 우선 사용하며, 외부 패키지가 필요하면 사용 이유와 설치 방법을 함께 적는다.
- 파일명과 폴더명은 내용을 쉽게 짐작할 수 있도록 일관되게 정한다.
- 기존 파일을 수정할 때는 사용자의 변경 사항을 보존하고 요청 범위 밖의 내용은 바꾸지 않는다.

## R 코드 스타일

- 들여쓰기는 공백 2칸을 사용한다.
- 대입에는 특별한 이유가 없으면 `<-`를 사용한다.
- 변수와 함수 이름은 의미가 분명한 `snake_case`로 작성한다.
- 연산자와 쉼표 뒤에는 공백을 넣는다.
- 함수는 한 가지 역할만 수행하도록 짧게 작성한다.
- 입문자가 이해하기 어려운 축약 표현이나 과도한 파이프 연결은 피한다.
- 주석은 코드 자체로 드러나지 않는 의도나 학습 포인트를 설명할 때만 사용한다.

## 권장 구조

- `README.md`: 학습 목표, 실행 환경, 전체 목차
- `lessons/`: 주제별 수업 자료
- `examples/`: 바로 실행할 수 있는 R 예제
- `exercises/`: 연습 문제와 필요한 데이터
- `data/`: 예제용 데이터와 출처 설명
- `tests/`: 함수나 예제의 자동 검증 코드

프로젝트가 작을 때는 필요하지 않은 폴더를 미리 만들지 않는다.

## 실행 및 검증

- 변경한 R 스크립트는 가능하면 `Rscript <파일명>`으로 직접 실행한다.
- 예제는 깨끗한 R 세션에서도 같은 결과를 낼 수 있어야 한다.
- 외부 파일을 사용할 때는 절대 경로 대신 프로젝트 루트 기준의 상대 경로를 사용한다.
- 난수 예제는 재현이 필요하면 `set.seed()`를 명시한다.
- 패키지를 추가한 경우 해당 패키지가 설치되지 않았을 때의 준비 방법을 문서화한다.
- 실행 환경에 R이 없거나 검증할 수 없는 경우 완료 보고에 그 사실을 명확히 밝힌다.

## 문서 작성

- 학습 목표와 선수 지식을 문서 앞부분에 적는다.
- 코드 블록에는 실행 가능한 완전한 예제를 넣는다.
- 예상 결과가 학습에 중요하면 코드 바로 아래에 함께 설명한다.
- 데이터 출처, 라이선스, 외부 참고 자료는 링크와 함께 기록한다.
- 같은 용어는 문서 전체에서 동일한 번역과 표기를 사용한다.

## 변경 완료 전 확인

1. 요청한 범위만 변경했는지 확인한다.
2. 새 코드와 예제를 실제로 실행하거나 문법 오류를 점검한다.
3. 문서의 경로, 명령어, 코드가 현재 저장소 구조와 일치하는지 확인한다.
4. 변경한 파일과 검증 결과를 간결하게 보고한다.
